# Dependencies — must never be copied; pnpm installs them in the deps stage
node_modules
**/node_modules
.pnpm-store

# Build outputs
.next
**/.next
out
dist
build
**/dist
**/build

# Local data / uploads
uploads
.docker
# Whitelist: bake the entrypoint + minio-init scripts into the api image.
# (`.docker` is excluded above to drop data dirs; the scripts still need to
# reach the build context.)
!.docker/entrypoint.sh
!.docker/minio-init.sh
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env files
.env
.env.local
.env.*.local

# Misc
.git
.gitignore
.vscode
.idea
coverage
*.tsbuildinfo
next-env.d.ts